Update personas by pulling the latest changes from the repository and reinstalling.

On Windows, run `update.ps1` in the repository root.

On Linux/Mac, run `update.sh` in the repository root.

Report the output to the user.
