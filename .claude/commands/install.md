Install personas from this repository as slash commands.

On Windows, run `install.ps1` in the repository root. Pass `-Adult` if the user wants explicit-rated personas included.

On Linux/Mac, run `install.sh` in the repository root. Pass `--adult` if the user wants explicit-rated personas included.

Report the output to the user.
